import { createTheme } from "@material-ui/core";

const theme = createTheme({
    palette: {
      primary: {
        main: '#78909c',
      },
      secondary: {
        main: '#ffb300',
        dark: '#ff9800',
      },
    }});

export default theme;
